# Copie para .env local (netlify dev) — não commitar o .env
# No Netlify: Site settings → Environment variables

CESIUM_API_KEY=your_cesium_ion_access_token
# Opcional — se o Ion exigir chave Google Maps (Map Tiles API):
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key
